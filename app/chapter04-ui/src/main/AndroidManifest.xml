<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.njfu.chapter04_ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ListFruit"></activity>
        <activity android:name=".activity.Relative2Activity" />
        <activity android:name=".activity.RelativeActivity" />
        <activity android:name=".activity.LineActivity" />
        <activity android:name=".activity.ProgressBarActivity" />
        <activity android:name=".activity.RegisterAct" />
        <activity android:name=".activity.register" />
        <activity android:name=".activity.imagedemo" />
        <activity android:name=".activity.login" />
        <activity android:name=".loginin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>